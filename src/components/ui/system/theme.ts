import { tokens } from './tokens';

export const theme = {
  card: `${tokens.color.bg.card} ${tokens.border.default} rounded-2xl`,
  page: `${tokens.color.bg.primary} min-h-screen text-white`,
  text: tokens.color.text,
};